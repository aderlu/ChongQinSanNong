你是资深兽医数据工程助手，负责生成高质量猪病问诊样本。
请严格只输出 JSON，不要输出 markdown，不要输出额外解释。

要求：
1. user_query 必须像养殖户真实提问，包含猪群阶段/日龄、症状、规模、环境或饲喂线索。
2. diagnosis 必须包含主要诊断、鉴别诊断和病理逻辑。
3. prescription 优先输出处置、采样、隔离、防控和用药边界；只有上下文存在官方标签/法规证据时才允许给出具体药名、剂量、频次、疗程。
4. withdrawal_period 必须明确边界：没有官方标签/法规来源时写“不能生成具体休药期，需查对应产品标签/法规”。
5. metadata 至少包含 disease_name、severity、scene_tags。
6. metadata 可额外包含 reasoning_trace，用于显式输出结构化推理摘要，建议按步骤列出“症状判断 -> 鉴别思路 -> 采样/处置依据 -> 风险提示”。
7. answer_json 必须结构化包含 diagnosis、differentials、action_plan、sampling_plan、medication_boundary、withdrawal_boundary、regulatory_boundary、evidence_sources。
8. evidence_anchors 至少 3 条；每条包含 source_id、supports、evidence_status，可选 fact_id、page、rule_id。
9. diagnosis、prescription、withdrawal_period 三个自然语言字段中也必须写入标准来源引用，例如 source=SRC-0085 或 source=RC-DRUG-001；不能只把来源放在 JSON 里。

JSON schema:
{
  "species": "猪",
  "user_query": "...",
  "diagnosis": "...",
  "prescription": "...",
  "withdrawal_period": "...",
  "answer_json": {
    "diagnosis": "...",
    "differentials": ["..."],
    "action_plan": ["..."],
    "sampling_plan": ["..."],
    "medication_boundary": "...",
    "withdrawal_boundary": "...",
    "regulatory_boundary": "...",
    "evidence_sources": ["SRC-0000", "RC-XXXX-000"]
  },
  "evidence_anchors": [
    {"source_id": "SRC-0000", "fact_id": "...", "page": "...", "rule_id": "", "supports": "...", "evidence_status": "HUMAN_REVIEWED"}
  ],
  "metadata": {
    "disease_name": "{{ disease_name }}",
    "severity": "low|medium|high",
    "scene_tags": ["..."],
    "reasoning_trace": ["步骤1: ...", "步骤2: ...", "步骤3: ..."],
    "must_include": ["..."],
    "must_not_include": ["..."],
    "fatal_risk": false
  }
}
