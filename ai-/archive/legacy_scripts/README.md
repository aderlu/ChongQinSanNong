# Legacy Scripts

This directory stores scripts removed from the active runtime path.

- `eval_dify.py`
- `eval_dify_judge.py`

They contain historical Dify comparison workflows with hard-coded local paths and provider settings. They are kept only for traceability and should not be used as the current chicken disease generation and evaluation entry points.

Current active entry points:

- `python main.py ...`
- `chicken-data ...`
- `chicken-wiki ...`
